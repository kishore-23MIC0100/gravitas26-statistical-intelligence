export type Row=Record<string,string>; export type Mapping=Partial<Record<"type"|"college"|"event"|"eventId"|"category"|"registration"|"internationalEvent"|"country"|"internationalParticipant"|"payment"|"gender",string>>;
export type Metric<T>={value:T|null;status:"valid"|"unavailable"|"insufficient";note?:string;records:number}; export type Report={rows:number;columns:string[];mapping:Mapping;quality:{duplicates:number;missing:number;status:string};internal:Metric<number>;external:Metric<number>;externalColleges:Metric<{name:string;count:number}[]>;categories:Metric<{name:string;count:number}[]>;ranking:Metric<{name:string;category:string;count:number}[]>;internationalEvents:Metric<number>;internationalParticipants:Metric<number>;paid:Metric<number>;genders:Metric<{name:string;count:number;percent:number}[]>};
const aliases:Record<keyof Mapping,string[]>={type:["participant type","college type","internal/external","registration type","type"],college:["college name","college","institution"],event:["event name","event"],eventId:["event id","event code"],category:["event category","category"],registration:["registration id","registration","participant id"],internationalEvent:["international flag","international event","event type","international status"],country:["participant country","country","nationality"],internationalParticipant:["international participant flag","international participant"],payment:["payment status","transaction status","payment confirmation","payment"],gender:["gender","sex"]};
const norm=(x:string)=>x.trim().toLowerCase().replace(/[_-]/g," ").replace(/\s+/g," "); const yes=(x:string)=>["yes","true","international","intl","1"].includes(norm(x)); const paid=(x:string)=>["paid","success","successful","confirmed","completed"].includes(norm(x));
export function detectMapping(columns:string[]):Mapping{const m:Mapping={};for(const key of Object.keys(aliases) as (keyof Mapping)[]){const f=columns.find(c=>aliases[key].includes(norm(c)));if(f)m[key]=f}return m}
const mk=(value:unknown,records:number,note?:string):Metric<any>=>({value,status:value===null?"unavailable":records?"valid":"insufficient",note,records});
function groups(rows:Row[],field:string,distinct?:string){const m=new Map<string,Set<string>>();rows.forEach((r,i)=>{const k=r[field]?.trim();if(!k)return;if(!m.has(k))m.set(k,new Set());m.get(k)!.add(distinct&&r[distinct]?r[distinct]:String(i))});return [...m].map(([name,s])=>({name,count:s.size})).sort((a,b)=>b.count-a.count||a.name.localeCompare(b.name))}
export function analyze(input:Row[],columns:string[],override:Mapping={}):Report{const mapping={...detectMapping(columns),...override},rows=input.filter(r=>Object.values(r).some(v=>v.trim()));const seen=new Set<string>(),dups=new Set<string>();rows.forEach(r=>{const k=JSON.stringify(r);if(seen.has(k))dups.add(k);seen.add(k)});const missing=rows.filter(r=>Object.values(mapping).some(f=>f&&!r[f]?.trim())).length;const type=mapping.type,col=mapping.college,event=mapping.event||mapping.eventId,cat=mapping.category,reg=mapping.registration,intE=mapping.internationalEvent,intP=mapping.internationalParticipant,country=mapping.country,pay=mapping.payment,gender=mapping.gender;const internal=type?rows.filter(r=>norm(r[type])==="internal"):[],external=type?rows.filter(r=>norm(r[type])==="external"):[];const rank=event?groups(rows,event,reg).map(x=>({...x,category:cat?rows.find(r=>r[event]===x.name)?.[cat]||"—":"—"})):[];const intEvents=intE&&event?new Set(rows.filter(r=>yes(r[intE])).map(r=>r[event]).filter(Boolean)).size:0;const intParticipants=intP?rows.filter(r=>yes(r[intP])).length:country?rows.filter(r=>{const x=norm(r[country]);return x&&!['india','indian','in'].includes(x)}).length:0;const gs=gender?groups(rows,gender,reg).map(x=>({...x,percent:Number((x.count/rows.length*100).toFixed(1))})):[];return{rows:rows.length,columns,mapping,quality:{duplicates:dups.size,missing,status:dups.size||missing?"Review Required":"Valid"},internal:type?mk(internal.length,internal.length):mk(null,0,"Map an Internal/External field."),external:type?mk(external.length,external.length):mk(null,0,"Map an Internal/External field."),externalColleges:type&&col?mk(groups(external,col,reg),external.length):mk(null,0,"Map both type and college fields."),categories:cat&&event?mk(groups(rows,cat,event),rows.length):mk(null,0,"Map category and event fields."),ranking:event?mk(rank,rows.length):mk(null,0,"Map an event field."),internationalEvents:intE&&event?mk(intEvents,intEvents):mk(null,0,"Map international-event and event fields."),internationalParticipants:intP||country?mk(intParticipants,intParticipants):mk(null,0,"International participant count cannot be determined from the available dataset."),paid:pay?mk(rows.filter(r=>paid(r[pay])).length,rows.length):mk(null,0,"Map a payment-status field."),genders:gender?mk(gs,rows.length):mk(null,0,"Map a gender field.")}}
